apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: azure-store
  namespace: testapp
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: "https://xxxxx.vault.azure.net" # Change
      serviceAccountRef:
        name: external-secret-service-account